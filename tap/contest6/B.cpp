#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const int N = 20010;
ll v[N];

int main(){
    ios_base :: sync_with_stdio(false);
    cin.tie(nullptr);
    int n;

    cin >> n;

    for(int i = 0; i < n; i++)
        cin >> v[i];

    
    

    return 0;
}